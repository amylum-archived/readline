Prospectus.extra_dep('file', 'pkgforge')
Prospectus.extra_dep('file', 'prospectus_pkgforge')

item do
  expected do
    url_xpath
    url 'http://git.savannah.gnu.org/cgit/readline.git/refs/tags'
    xpath '//*[@id="cgit"]/div/table/tr[2]/td[1]/a'
    regex /^readline-([.\d]+)/
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end

  extend ProspectusPkgforge::DepItems
end
